
import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    root: {
        // maxWidth: 345,
        height: 508,
        borderRadius: '22px',
        background: '#F3F8FC'
    },
    media: {
        height: 270,
    },
}));